{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <ul>
        {% for event in events %}
            <li> <a href="/event/{{ event.id }}">{{ event.name }}</a></li>
        {% endfor %}
    </ul>

    <a href="{{path('app_event_create')}}">Creer un evenement</a>

    <a href="/">Retour au home</a>
{% endblock %}
